package com.greenicephoenix.traceledger.feature.widget

/**
 * Shared constants used between TraceLedgerWidget and MainActivity.
 * Both sides read the same string key — keeping it here prevents typos.
 */
object WidgetConstants {
    const val EXTRA_NAVIGATE_TO_ADD = "navigate_to_add_transaction"
}