<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <!--
        Android 11+ (API 30+) Package Visibility fix.
        Without this block, startActivity() for https:// or upi:// is silently
        dropped on Android 11+ — no crash, no exception, just nothing happening.
        This is why the PayPal button appeared to do nothing.
    -->
    <queries>
        <!-- Browsers — required for PayPal and all other https:// links -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
        <!-- UPI apps — GPay, PhonePe, Paytm, BHIM etc. -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="upi" />
        </intent>
    </queries>

    <application
        android:name=".TraceLedgerApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TraceLedger">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.TraceLedger">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            DailyReminderReceiver handles two intents:
              1. DAILY_REMINDER — AlarmManager fires at the scheduled time
              2. BOOT_COMPLETED — re-schedule after device reboot
            exported="true" is required for BOOT_COMPLETED.
        -->
        <receiver
            android:name=".core.notifications.DailyReminderReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.greenicephoenix.traceledger.DAILY_REMINDER" />
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!--
            FileProvider converts a raw file path into a content:// URI so the
            package installer can safely read the downloaded update APK.
            Required on Android 7+ — file:// URIs cause FileUriExposedException.
        -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_provider_paths" />
        </provider>

    </application>

</manifest>